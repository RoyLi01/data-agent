# 登记指标查询

只使用指标注册表中的定义。查新增必须区分注册与激活。留存明确注册 cohort 和第 7 自然日。时间采用 Asia/Shanghai 自然日，包含起止日期。按成本拆操作系统时拒绝未经定义的分摊。受控模板不能通过改 SQL 绕过。拿不到数据时报告问题，不编造数值。

步骤：明确契约；检索元数据并补齐依赖；校验；调用只读查询；登记来源 SQL、范围、水位和完整性。SQL 错误有限重试，数据不完整不重试成另一种口径。
