# 实施状态与续作检查点

更新：2026-09-23。代码目录 `/Users/ruoyangli/Desktop/大模型/Data Agent/`。旧 RAG 项目只读。

## 已完成并验证

- 5 张模拟业务表：注册、激活、活跃、渠道、花费。固定随机种子，含失败/重复/跨日事件。
- 5 类指标：注册人数、成功激活人数、注册七日激活率、D7 留存、激活成本。
- 表/字段/指标/关系结构化知识单元，BM25 + 词法哈希向量 + RRF + 词重叠重排，指标依赖补全。
- Python 状态机、查询/分析角色边界、按需加载 Skills、工具白名单和调用预算。
- 真实 MCP stdio 客户端和服务端，动态工具发现与调用，受控只读查询。
- SQLGlot AST、字段绑定、表访问范围、指标模板一致性、实际执行超时、结果行数上限。
- 澄清任务持久化、版本检查和幂等恢复；进程重启后保留待澄清任务，其他中断任务标记失败，不自动重复查询。
- 历史结果契约、快照、水位、TTL、完整性和所有者校验。缺维度重新查询；完整渠道结果可筛选复用。
- 固定统计、柱状图、分组趋势图、来源 SQL、变换记录和 Markdown 报告。
- FastAPI 接口、问数界面、Token 可选访问限制、跨站写请求拒绝。
- 浏览器验收：取数、追问复用绘图、歧义澄清、页面刷新恢复、澄清后继续查询均通过。
- Docker Compose 配置语法检查通过；容器构建和运行仍待 Docker daemon 可用。
- 42 项自动化测试通过；30 查询、8 多轮、2 澄清开发回归场景通过。SQL 结果用独立 Python 算法对照。

## 已有代码，但必须联调后才能算验收

- 真实模型 Tool Calling：查询计划结构化输出及分析展示决策。需用户在本地配置模型服务。
- 远程 Embedding、Reranker、Milvus 可选存储。离线哈希向量不是语义模型。
- Dockerfile、Compose 配置。当前 Docker daemon 未启动，尚未构建和启动镜像。

## 尚未实现，不应写成已完成成果

- ClickHouse 数据执行与 PostgreSQL 状态存储（当前都是 SQLite）。
- 任意自由 SQL 的生成和修复：当前只执行已登记指标模板；支持非法 SQL 拦截，不声称通用语义等价校验。
- 任意 Python 代码沙箱：当前统计、绘图使用固定函数。
- 企业多租户认证、真实行列权限、组织结构及审计接入。
- 多阶段复杂业务规划、自动探索式分析、同比环比成对查询、因果归因。
- 独立真实模型盲测、完整消融实验、线上成本/性能数据。

## 目前需要用户提供的内容

开发与离线验收不需要更多信息。真实模型联调需要服务商、模型名、Base URL，以及用户自行写入 `.env` 的 API Key。不要在聊天或版本库中暴露密钥。若要验收容器部署，需 Docker Desktop daemon 正常运行。

## 下一轮顺序

1. 根据业务首版反馈修正口径/演示问题；不要扩大未验证的简历声明。
2. 接入用户选定的模型与向量服务，先跑少量真实问题，验证 JSON Schema、工具调用和协议差异。
3. 建独立保留测试集，分别记录语义召回、计划、执行结果、澄清及复用决策。
4. Docker 环境可用时验证容器；按需要增加外部数仓/状态后端和代码隔离。

## 额度约定

用户要求为 5 小时额度保留 10%。长任务查询账户共享额度；剩余约 15% 时保存检查点，不再开始大型步骤；接近 10% 时暂停。工具返回的额度百分比不是精确 Token 数，无法阻止其他任务同时消耗账户额度。
